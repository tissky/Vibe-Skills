# VCO Release v2.3.42

- Date: 2026-03-14
- Commit(base): 0ad2a21

## Highlights

- Converted the Linux full-authoritative candidate proof bundle from author-machine residue into a distributable governed artifact by tracking the frozen `environment.log`, `install.log`, `check.log`, `bootstrap-doctor.log`, `coherence.log`, and Docker `provision.log` evidence files.
- Added `scripts/verify/vibe-proof-bundle-tracked-files-gate.ps1` so every manifest-declared fresh-machine report, operation record, and proof artifact must be both present and git-tracked before Linux promotion truth can be called green.
- Wired the new tracked-files gate into `scripts/verify/vibe-platform-promotion-bundle.ps1`, so platform promotion can no longer pass because of ignored local evidence that disappears in a clean clone.
- Kept Linux public support truth conservative at `supported-with-constraints` while correcting the release-truth mismatch that affected `v2.3.41`.

## Validation Notes

- Verified from the canonical repo root after converting the proof artifacts into tracked repository content:
  - `scripts/verify/vibe-proof-bundle-tracked-files-gate.ps1`
  - `scripts/verify/vibe-linux-pwsh-proof-gate.ps1`
  - `scripts/verify/vibe-platform-promotion-bundle.ps1`
  - `scripts/verify/vibe-mirror-edit-hygiene-gate.ps1`
  - `scripts/verify/vibe-dist-manifest-gate.ps1`
  - `scripts/verify/vibe-version-consistency-gate.ps1`
  - `scripts/verify/vibe-version-packaging-gate.ps1`
- Verified a clean staged export no longer loses Linux proof-bundle evidence to `.gitignore`, which means the proof/promotion verdict now follows versioned repository contents instead of local author residue.

## Migration Notes

- `v2.3.41` remains historically useful for the target-root portability hardening itself, but its Linux proof-green claim is superseded by this release because the prior proof bundle was not fully distributable.
- Linux still remains `supported-with-constraints`; this release fixes release-truth and proof-bundle distribution, not the public platform status.
- Future proof bundles must keep manifest-declared artifacts tracked, or `vibe-proof-bundle-tracked-files-gate.ps1` and `vibe-platform-promotion-bundle.ps1` will fail.
