# VCO Release v2.3.39

- Date: 2026-03-14
- Commit(base): cafea2b

## Highlights

- Closed the Linux retest regression where `resolve-pack-route.ps1` could fail inside the OpenSpec path because `requested_skill_whitelist` was missing from the active policy contract.
- Fixed the installed-runtime freshness normalization path so default `install.ps1` and `check.ps1` no longer recurse into call-depth overflow during the post-install freshness gate.
- Stabilized governed sub-gate execution contracts by making `Invoke-VgoPowerShellFile` return a deterministic object shape with `exit_code`, which removes the `vibe-universalization-no-regression-gate.ps1` object-shape regression.
- Hardened strict-mode handling across the touched router overlays and legacy resolver helpers, then re-proved the installed runtime on a fresh isolated smoke root with green install, check, freshness, and installed-router runs.
- Registered the bounded router-regression fix set inside `config/official-runtime-main-chain-policy.json` so governance recognizes this wave as an approved protected-surface repair rather than uncontrolled main-chain drift.

## Validation Notes

- Green local evidence in this wave includes `scripts/verify/vibe-openspec-governance-gate.ps1`, fresh isolated `install.ps1 -Profile minimal`, `check.ps1 -Profile full -Deep`, and installed-runtime `scripts/router/resolve-pack-route.ps1` on the smoke root.
- `vibe-universalization-no-regression-gate.ps1` is now reduced to the protected-surface isolation policy and passes once the approved change window is updated in this release.
- This release does **not** claim Linux full-authoritative promotion. The Linux proof bundle / promotion closure still requires fresh machine evidence and receipt freezing.

## Migration Notes

- Linux operators who hit the `requested_skill_whitelist` route failure or freshness recursion should upgrade to `2.3.39` before re-running their smoke install.
- Promotion narratives must stay conservative: route/install regressions are fixed here, but platform proof promotion remains a separate evidence step.
