# VCO Release v2.3.31

- Date: 2026-03-13
- Commit(base): 83b7be2

## Highlights

- Closed the three governed rollout plans for distribution governance, post-upstream repo convergence, and developer-entry hardening with fresh verification evidence.
- Fixed external-corpus tracked output fixture drift so `vibe-output-artifact-boundary-gate.ps1` is green again.
- Fixed third-party disclosure canonicalization so repository-local document links no longer trigger false undeclared-upstream findings.
- Re-cut the governed release surface and aligned canonical, bundled, and installed-runtime assets onto `v2.3.31`.

## Migration Notes

- No runtime feature rollback is intended in this release; the changes are governance, packaging, disclosure, and release-surface closure work.
- Operators should rerun `install.ps1` or `check.ps1` against the target Codex root so the installed runtime picks up `v2.3.31` and the refreshed governance artifacts.
- GitHub consumers should treat `v2.3.31` as the canonical post-upstream-governance closure point for local repo, bundled mirror, and installed runtime alignment.
