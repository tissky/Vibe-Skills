# 发布 v2.3.56 architecture-closure baseline 需求

**日期**: 2026-04-04
**目标**: 在新分支 `release/v2.3.56-architecture-closure-baseline` 上，把已经完成的 architecture-closure 收口整合为新的本地版本基线 `v2.3.56`，让当前版本面、release notes、README、changelog、ledger 与 release-facing manifests 指向同一份真相。

## Intent Contract

- Goal: 把当前已验证的低耦合高内聚架构收口结果，整合成一个清晰、可继续开发、可作为 GitHub 下一个版本基线的本地 release surface。
- Deliverable:
  - 新版本分支 `release/v2.3.56-architecture-closure-baseline`。
  - `config/version-governance.json`、`SKILL.md`、`dist/*` release-facing manifest 的版本面更新到 `v2.3.56 / 2026-04-04`。
  - `docs/releases/v2.3.56.md` 的正式版本说明。
  - `docs/releases/README.md`、`references/changelog.md`、`references/release-ledger.jsonl` 的同步更新。
  - release baseline 所需的最小验证与 cleanup 证据。
- Constraints:
  - 这次只完成本地版本基线，不声称已经创建远端 tag / GitHub release。
  - 版本说明只能写已经在当前 worktree 中完成并验证过的内容，不能把后续 backlog 写成已交付能力。
  - 历史 proof bundle、历史 release notes、历史 evidence 不伪装成 `v2.3.56`。
  - 不能回滚或覆盖当前已完成的 architecture-closure 收口结果。
- Acceptance Criteria:
  - 当前工作分支是 `release/v2.3.56-architecture-closure-baseline`。
  - `config/version-governance.json`、`SKILL.md`、`docs/releases/README.md`、`docs/releases/v2.3.56.md`、`references/changelog.md`、`references/release-ledger.jsonl` 与 release-facing dist manifests 对齐到 `v2.3.56 / 2026-04-04`。
  - `docs/releases/v2.3.56.md` 准确覆盖本版核心内容：remaining-architecture-closure 完成、owner->consumer proof、live status/proof alignment、bounded fallback honesty、full regression evidence。
  - focused release-surface verification 通过。
- Product Acceptance Criteria:
  - 团队可以把这版作为下一个 GitHub 基线来继续开发。
  - 用户读 `docs/releases/v2.3.56.md` 能理解本版是“架构收口与真相对齐版”，而不是功能扩展版。
- Manual Spot Checks:
  - 检查 `docs/releases/README.md` 已把 `v2.3.56` 设为 current release surface。
  - 检查 release note 与 `current-state.md` / `closure-audit.md` 的完成语言一致。
  - 检查 changelog / ledger / manifests 没有把本地基线误写成已远端发布。
- Completion Language Policy:
  - 在完成版本面更新、release note、验证、cleanup 之前，只能说“`v2.3.56` 本地版本基线正在收口”。
  - 完成这些步骤后，只能说“`v2.3.56` 本地版本基线已完成”，不能说“已正式发布到 GitHub”。
- Delivery Truth Contract:
  - 本轮只交付本地 release baseline truth，不交付远端 release object truth。
  - 本轮只覆盖当前已完成的 architecture-closure 收口结果和相关版本面对齐。
- Non-goals:
  - 不创建远端 tag。
  - 不创建 GitHub release。
  - 不开启新的后续 backlog 主题（outputs strict-mode、third_party parameterization、archive/prune、blanket shim retirement）。
- Autonomy Mode: `interactive_governed`
- Inferred Assumptions:
  - 现有稳定版本面仍停在 `v2.3.55`，因此合理的下一个本地基线版本号是 `v2.3.56`。
  - 用户当前要的是“新版本专用分支 + 版本面整合”，而不是立即远端发版。
