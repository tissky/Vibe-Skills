# 发布 v2.3.56 architecture-closure baseline 执行计划

**日期**: 2026-04-04
**需求文档**: [../requirements/2026-04-04-release-v2.3.56-architecture-closure-baseline.md](../requirements/2026-04-04-release-v2.3.56-architecture-closure-baseline.md)
**Internal Grade**: `L`

## Wave Structure

### Wave 1: Freeze Local Release Baseline Scope

- 冻结 `v2.3.56` requirement / plan。
- 确认当前 branch 为 `release/v2.3.56-architecture-closure-baseline`。
- 确认本轮只做本地版本基线，不做远端发布宣称。

### Wave 2: Update Version Surface

- 更新 `config/version-governance.json` 的 release summary 到 `v2.3.56 / 2026-04-04`。
- 更新 `SKILL.md` maintenance marker。
- 同步 release-facing `dist/*` manifests 的 `source_release`。

### Wave 3: Release Notes And Navigation

- 创建 `docs/releases/v2.3.56.md`。
- 更新 `docs/releases/README.md` current release surface。
- 更新 `references/changelog.md` 与 `references/release-ledger.jsonl`。

### Wave 4: Verification

- 运行 release surface focused verification。
- 运行 `git diff --check`。
- 保留 architecture-closure full regression 结果作为本版 baseline evidence。

### Wave 5: Cleanup And Handoff

- 清理临时文件。
- 审计 table5-owned `pytest` / `pwsh` / `node` 进程。
- 输出版本基线完成结论与边界说明。

## Verification Commands

- `python3 tools/build/sync_dist_release_manifests.py --repo-root .`
- `pwsh -NoProfile -File scripts/verify/vibe-version-packaging-gate.ps1`
- `pwsh -NoProfile -File scripts/verify/vibe-dist-manifest-gate.ps1`
- `pwsh -NoProfile -File scripts/verify/vibe-release-notes-quality-gate.ps1`
- `git diff --check`

## Completion Language Rules

- 没有完成版本面、release notes、focused verification、cleanup 之前，只能说“正在收口 `v2.3.56` 本地版本基线”。
- 完成后只能说“`v2.3.56` 本地版本基线已完成”，不能说“GitHub release 已创建”。

## Cleanup Expectations

- 不引入新的 table5 临时文件残留。
- 不碰历史版本 release note 的正文内容。
- 不修改历史 proof bundle 的版本记录。
