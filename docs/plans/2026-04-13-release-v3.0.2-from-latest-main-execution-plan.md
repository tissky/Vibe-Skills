# VibeSkills v3.0.2 Release Execution Plan

## Execution Summary

Cut a governed `v3.0.2` release from `main` at `c2f98b5`, refresh the repository-owned release surfaces through the canonical release operator, verify the resulting state, then publish the git tag and GitHub release.

## Frozen Inputs

- Requirement doc: `docs/requirements/2026-04-13-release-v3.0.2-from-latest-main.md`
- Release base: `c2f98b53890dfecb3fd6160c0e690d93f7fb5fe4`
- Latest public release: GitHub `v3.0.1` at `d94b772434b37a1ec817a039dfb900c4ffdbf86a`
- Release date: `2026-04-13`

## Anti-Proxy-Goal-Drift Controls

Prefill from the frozen requirement doc where available. Only diverge with explicit justification.

### Primary Objective

Publish a truthful `v3.0.2` release from the current `main` line.

### Non-Objective Proxy Signals

- Placeholder release notes
- Local-only tags
- Unverified publication claims

### Validation Material Role

Verification outputs, git refs, and GitHub release metadata determine completion.

### Declared Tier

Strict

### Intended Scope

Release-owned repository surfaces plus remote publication metadata.

### Abstraction Layer Target

Governed release surfaces and GitHub release publication.

### Completion State Target

Local release cut complete, verification rerun complete, tag pushed, GitHub release published.

### Generalization Evidence Plan

Capture release notes, version metadata, verification receipts, tag metadata, and GitHub release metadata.

## Internal Grade Decision

L

The work is tightly coupled and sequence-sensitive. Version freeze, release note authoring, release-cut apply, verification, commit/tag, and GitHub publication should stay serial.

## Wave Plan

1. Freeze runtime artifacts for `skeleton_check`, `deep_interview`, `requirement_doc`, and `xl_plan`.
2. Inspect the latest `main` release delta and draft `docs/releases/v3.0.2.md` before running the apply step so the release note quality gate has real content.
3. Run `release-cut.ps1` in preview mode for `3.0.2`, inspect the planned edits, then run the apply path.
4. Run targeted release verification commands and review the final diff.
5. Commit the release cut, create tag `v3.0.2`, push commit/tag, and create the GitHub release object from the authored notes.
6. Emit execution and cleanup receipts.

## Delivery Acceptance Plan

- `docs/releases/README.md` must point to `v3.0.2` as current.
- `docs/releases/v3.0.2.md` must have concrete highlights, validation, and migration sections.
- `references/release-ledger.jsonl` tail must record `v3.0.2` on the release commit.
- `gh release view v3.0.2` must resolve successfully after publication.

## Artifact Review Strategy

- Review the authored release note before publication.
- Verify the generated release README, changelog head, and ledger tail after `release-cut` apply.
- Confirm the created tag and GitHub release metadata after publish.

## Code Task TDD Evidence Plan

Prefill this section from the frozen requirement doc. Map each frozen requirement or exception to implementation, targeted verification, and proof artifacts. Only diverge with explicit justification.

No code-task TDD evidence was frozen. Verification will rely on governed release gates and targeted tests instead.

## Baseline Document Quality Mapping

- Concrete release note content: author the note before running apply, then inspect the final file after apply.
- Honest comparison language: explicitly describe `v3.0.1` as the latest public side-release and `v3.0.2` as the latest `main`-line publication.
- Real validation and migration guidance: record actual commands and actual release-facing implications after verification completes.

## Baseline UI Quality Mapping

No baseline UI quality dimensions were frozen for this run.

## Task-Specific Acceptance Mapping

- Keep package metadata untouched unless verification indicates that release-owned conventions changed.
- Avoid hand-editing generated dist release manifests outside the governed operator; let `release-cut.ps1` own them.

## Research Augmentation Plan

- Use local git history to summarize release themes.
- Use GitHub release metadata to anchor the comparison against public `v3.0.1`.

## Completion Language Rules

- "Release cut" means repository surfaces are updated and verification passed locally.
- "Published" means the commit/tag were pushed and a GitHub release object exists.
- "Latest source" refers to `main` at `c2f98b5`, not the older `v3.0.1` side-release commit.

## Ownership Boundaries

- Release metadata surfaces:
  `config/version-governance.json`, `SKILL.md`, `references/changelog.md`, `references/release-ledger.jsonl`, `docs/releases/README.md`, `docs/releases/v3.0.2.md`, generated `dist/**` manifests owned by the release operator.
- Governance artifacts:
  `docs/requirements/2026-04-13-release-v3.0.2-from-latest-main.md`, `docs/plans/2026-04-13-release-v3.0.2-from-latest-main-execution-plan.md`, `outputs/runtime/vibe-sessions/20260413T105521Z-release-v3-0-2-main/*`.
- GitHub publication surfaces:
  release commit, tag `v3.0.2`, GitHub release object.

## Verification Commands

- `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/governance/release-cut.ps1 -Version 3.0.2 -Updated 2026-04-13 -Preview -RunGates`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/governance/release-cut.ps1 -Version 3.0.2 -Updated 2026-04-13 -RunGates`
- `python3 -m pytest -q tests/integration/test_release_cut_gate_contract_cutover.py tests/integration/test_dist_manifest_generation.py tests/runtime_neutral/test_release_notes_quality.py tests/runtime_neutral/test_release_cut_operator.py`
- `pwsh -NoProfile -File scripts/verify/vibe-version-consistency-gate.ps1`
- `pwsh -NoProfile -File scripts/verify/vibe-dist-manifest-gate.ps1`
- `pwsh -NoProfile -File scripts/verify/vibe-version-packaging-gate.ps1`
- `pwsh -NoProfile -File scripts/verify/vibe-release-notes-quality-gate.ps1`
- `git diff --check`
- `gh release view v3.0.2 -R foryourhealth111-pixel/Vibe-Skills --json tagName,targetCommitish,publishedAt,url`

## Rollback Plan

- If preview fails, stop before apply and fix the release surface or blocked gate.
- If apply fails, repair the failing governed surface in-place, rerun verification, and only then continue.
- If remote push or GitHub release creation fails, leave the verified local release commit intact and report the exact failed publication step.

## Phase Cleanup Contract

- Write execution and cleanup receipts under `outputs/runtime/vibe-sessions/20260413T105521Z-release-v3-0-2-main/`.
- Record verification commands and publication outcomes in the receipts.
- Leave the release branch state available for inspection after publication.
