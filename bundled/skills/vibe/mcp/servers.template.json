{
  "servers": {
    "github": {
      "mode": "plugin",
      "required": false,
      "note": "Use installed github plugin when available"
    },
    "context7": {
      "mode": "plugin",
      "required": false
    },
    "serena": {
      "mode": "plugin",
      "required": false
    },
    "scrapling": {
      "mode": "stdio",
      "command": "scrapling",
      "args": [
        "mcp"
      ],
      "required": false,
      "note": "Requires: python -m pip install \"scrapling[ai]\""
    },
    "claude-flow": {
      "mode": "stdio",
      "command": "claude-flow",
      "args": [
        "mcp",
        "start"
      ],
      "required": false
    }
  }
}
